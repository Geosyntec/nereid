name: Lint

on:
  push:
    branches:
      - develop
      - master
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.9
      - name: Install Linters
        run: pip install black isort mypy -c nereid/requirements_tests.txt
      - name: Run Linters
        run: bash scripts/lint.sh
